#include <stdlib.h>

/**
 * _abs - check if typed input is lowercase
 *
 * @c: takes parameter c type of int and ckeck of its lowercase
 *
 * Return: return 1 if lowercase else return 0
 */
int _abs(int c)
{
	return (abs(c));
}
